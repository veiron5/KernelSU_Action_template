name: Build KernelSU Pixel 6
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Build Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y git python3 python3-pip

      - name: Setup KernelSU
        run: |
          echo "Пытаемся интегрировать KernelSU в GKI..."
          # Здесь должен быть скрипт для 5.10 ядра
          curl -LSs "https://raw.githubusercontent.com" | bash -s main
